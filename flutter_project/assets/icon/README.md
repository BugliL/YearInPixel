# App Icon

Please save the "Year in Pixels" icon image here as `app_icon.png`.

The image should be:
- Format: PNG
- Size: At least 1024x1024 pixels
- With transparency if needed

After saving the image here, run:
```bash
cd ..
flutter pub get
flutter pub run flutter_launcher_icons
```
