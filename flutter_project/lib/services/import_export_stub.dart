// Stub implementation for non-web platforms
void downloadFile(String content, String fileName) {
  // This should never be called on non-web platforms
  throw UnimplementedError('downloadFile is only available on web');
}
